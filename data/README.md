# Validation data and tools

Run main method in GenerateNoiseFiles to generate .dat files.

`setup.sh` for setting up randomness validation tool.

`noise_test.sh` to run validation against .dat files.